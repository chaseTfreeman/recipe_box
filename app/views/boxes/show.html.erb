<% flash.each do |type, message| %>
  <p><%= type %>: <%= message %></p>
<% end %>
<div class="whitespace"></div>
<div class="box-profile">
<h2>
  <%= @box.title %>
  <h2>
  <h3><%= link_to "Edit", edit_box_path(@box) %>
  <%= link_to "Delete", box_path(@box),
                                        method: :delete,
                                        data: { confirm: 'Are you sure?' } %></h3>

<hr>
<div class="boxes">
<img class="recipe-box" src="<%= @box.img_url %>">
</div>
<p>Box Theme:</p><h4><%= @box.theme %></h4>

<div>
<h4> insert recipe image here<h4>
</div>
</div>
